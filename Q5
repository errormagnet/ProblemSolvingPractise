function staircase(n) {
    for(var i=0; i<n; i++){
        console.log(' '.repeat(n-i-1) + '#'.repeat(i+1));
    }
}

def staircase(n)
    i = 1
    while i <= n
        puts ' ' * (n-i) + '#' * (i)
        i+=1
    end
end
